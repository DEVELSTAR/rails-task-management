<%= column_chart Course.joins(:user_course_enrollments)
                       .group(:title)
                       .size,
                 height: "300px",
                 library: {title: {text: "Enrollments per Course"}} %>
